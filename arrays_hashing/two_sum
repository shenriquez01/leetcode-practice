class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        values_counted = {}

        for i, num in enumerate(nums):
            diff = target - num
            if diff in values_counted:
                return [values_counted[diff], i]
            else:
                values_counted[nums[i]] = i
